#include <stdio.h>
/**
 * main - print alphabet in lower case.
(*
 *Return: 0 on success
 */
int main(void)
{
	char a = 'c';
	while (c <= 'z')
	{
		if (c != 'q' && c != 'e')
		{
			putchar(c);
		}
		c++;
	}
	putchar('\n')
	return (0);
}
